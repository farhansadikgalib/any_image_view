name: any_image_view
version: 1.3.1

description: "A versatile Flutter image viewer supporting PNG, JPG, WebP, GIF, TIFF, RAW, SVG, Lottie, XFile, and cached network images — all in one seamless package."
topics:
  - flutter
  - image
  - image-viewer
  - svg
  - lottie

homepage: https://github.com/farhansadikgalib/any_image_view
repository: https://github.com/farhansadikgalib/any_image_view/blob/main/README.md
issue_tracker: https://github.com/farhansadikgalib/any_image_view/issues

screenshots:
  - description: "any image view"
    path: raw/ss.png

environment:
  sdk: ^3.2.6
  flutter: ">=3.10.0"

dependencies:
  lottie:
  flutter_svg:
  cached_network_image:
  cross_file:

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/images/