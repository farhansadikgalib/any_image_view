name: any_image_view
description: "any_image_view"
version: 1.0.0
homepage:

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  lottie:
  flutter_svg:
  cached_network_image:

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
    uses-material-design: true
    assets:
        - assets/images/


